
/**
 * 
 * foreach((value, index) => {
 * 
 * })
 * map
 * find
 * findIndex
 * filter
 * 
 * @param {any[]} arr 
 * @returns 
 */
function sum(arr) {
  return arr.reduce((a, b) => a + b)
}